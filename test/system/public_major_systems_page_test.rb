require "application_system_test_case"

class PublicMajorSystemsPageTest < ApplicationSystemTestCase
  test "displays the English major system per default" do
    skip "Implement once seed and fixtures are ready"
  end
end
